<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center" href="/">
      <i class="ri-questionnaire-line me-2"></i> QuizPlatform
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <% if (!user) { %>
          <li class="nav-item"><a class="nav-link" href="/login">Giriş Yap</a></li>
          <li class="nav-item"><a class="nav-link" href="/register">Kayıt Ol</a></li>
        <% } else { %>
          <% if (user.role === 'teacher' || user.role === 'admin') { %>
            <li class="nav-item"><a class="nav-link" href="/teacher/dashboard">Dashboard</a></li>
            <li class="nav-item"><a class="nav-link" href="/teacher/quizzes">Create Quiz</a></li>
          <% } else { %>
            <li class="nav-item"><a class="nav-link" href="/student/dashboard">My Quizzes</a></li>
            <li class="nav-item"><a class="nav-link" href="/student/quiz/<%= user.currentQuizId || '' %>?session=<%= user.sessionCode || '' %>">Play Quiz</a></li>
          <% } %>
          <li class="nav-item"><a class="nav-link" href="/auth/logout">Çıkış Yap</a></li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
